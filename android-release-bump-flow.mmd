flowchart TD
    START([🚀 Start Release]) --> PREFLIGHT

    PREFLIGHT["Pre-flight Checks<br>main branch · clean tree<br>keystore.properties"] -->|Fail| ABORT([❌ Abort])
    PREFLIGHT -->|Pass| DISCOVER

    DISCOVER["Discover App Name<br>tags → gradle → ask user"] --> ANALYZE

    ANALYZE["Analyze Commits<br>since last tag"] --> CP_VERSION

    CP_VERSION{{"✋ Version Recommendation<br>MAJOR / MINOR / PATCH"}}
    CP_VERSION -->|Approved| UPDATE

    UPDATE["Update build.gradle.kts<br>versionCode +1 · versionName"] --> NOTES

    NOTES["Generate Release Notes<br>auto-detect locales · 500 char limit"] --> CP_NOTES

    CP_NOTES{{✋ Review Release Notes}}
    CP_NOTES -->|Approved| VERIFY
    CP_NOTES -->|Edit| NOTES

    VERIFY["Pre-commit Verification<br>build · lint · test"] -->|Fail| ABORT_VERIFY([❌ Fix & retry])
    VERIFY -->|Pass| COMMIT

    COMMIT["Commit & Tag<br>version bump + git tag"] --> CP_PUSH

    CP_PUSH{{✋ Confirm Push}}
    CP_PUSH -->|Approved| PUSH

    PUSH["Push to origin/main<br>+ push tag"] --> BUNDLE

    BUNDLE[Build Signed Bundle\ncopyReleaseBundle] --> PLAY_CHECK

    PLAY_CHECK{"Upload to<br>Play Console?"}
    PLAY_CHECK -->|No| REPORT
    PLAY_CHECK -->|Yes| CP_UPLOAD

    CP_UPLOAD{{✋ Confirm Upload}}
    CP_UPLOAD -->|Approved| UPLOAD
    CP_UPLOAD -->|Skip| REPORT

    UPLOAD["Publish to<br>Internal Testing"] --> REPORT

    REPORT[/📋 Completion Report/] --> DONE([✅ Done])

    classDef checkpoint fill:#FFF3CD,stroke:#FFC107,stroke-width:2px,color:#856404
    classDef abort fill:#F8D7DA,stroke:#DC3545,stroke-width:2px,color:#721C24
    classDef success fill:#D4EDDA,stroke:#28A745,stroke-width:2px,color:#155724
    classDef report fill:#D1ECF1,stroke:#17A2B8,stroke-width:2px,color:#0C5460

    class CP_VERSION,CP_NOTES,CP_PUSH,CP_UPLOAD checkpoint
    class ABORT,ABORT_VERIFY abort
    class DONE success
    class REPORT report
